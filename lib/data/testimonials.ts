import type { Testimonial } from "@/types";

/**
 * No real client testimonials exist yet. Keep this array empty in production
 * so the testimonials section stays hidden (see components/sections/testimonials-section.tsx).
 * Populate it here, or swap this file for a CMS query, once real quotes are available.
 */
export const testimonials: Testimonial[] = [];
